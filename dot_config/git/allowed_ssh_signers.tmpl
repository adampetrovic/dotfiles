{{- if .personal -}}
{{ .email }} {{ (onepasswordRead "op://Private/SSH Key - Personal/public_key") }}
{{- else if .work -}}
# Work SSH signing is not configured until the Keeper migration supplies a signing key.
{{- end -}}
