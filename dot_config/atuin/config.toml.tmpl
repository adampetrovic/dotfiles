{{ if .personal -}}
sync_address = 'https://atuin.{{ (onepasswordRead "op://k8s/cluster-secrets/SECRET_DOMAIN") -}}'
{{ else if .work -}}
# Work profile: Atuin sync is not migrated to Keeper yet, so run local-only history.
{{ end -}}
enter_accept = false
search_mode = "fuzzy"
filter_mode = "host"

[keys]
scroll_exits = false
